[project]
name = "is459-crypto-bigdata-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websocket-client>=1.9.0",
    "websockets>=16.0",
]
